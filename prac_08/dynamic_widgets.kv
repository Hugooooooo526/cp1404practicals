BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        orientation: 'vertical'
        # 这个布局将在程序代码中填充Label小部件
        id: entries_box
    Label:
        size_hint_y: 0.1
        text: "以上是动态创建的标签"